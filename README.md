Multi-store E-commerce Admin (scaffold)

This workspace contains a starter scaffold for a multi-store e-commerce admin dashboard.

- Frontend: `jordan/web` (Vite + React)
- Backend: `jordan/server` (PHP API skeleton)

See the per-folder READMEs for setup instructions.
